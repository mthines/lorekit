// The `/learn/*` routes are now redirect stubs to `/docs/*`, so there is no
// content to skeleton. Redirects resolve before any fallback would show.
export default function LearnLoading() {
  return null;
}
