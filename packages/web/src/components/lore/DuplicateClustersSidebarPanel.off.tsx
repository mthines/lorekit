'use client';

/**
 * The `off` arm of `lore-explorer-duplicate-clusters` for the sidebar body —
 * see `DuplicateClustersPanel.off.tsx` for why an absent arm is a whole
 * component rather than a `null` literal inlined into the resolver.
 */

export function DuplicateClustersSidebarPanelOff() {
  return null;
}
