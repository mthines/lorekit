node_modules/
dist/
coverage/
.env.local
.env
*.env
.supabase/
supabase/.branches
supabase/.temp
.nx/cache
.nx/workspace-data
tmp/
**/.next/
**/.vercel/
**/next-env.d.ts
**/*.tsbuildinfo

.mcp.json

# Storybook build output
**/storybook-static/

# Vitest visual-regression run artifacts. The reference baselines
# (__screenshots__/**/*-chromium-linux.png) ARE committed; the per-run
# reference/actual/diff attachments Vitest writes on a mismatch are not.
**/.vitest-attachments/

# Autonomous workflow artifacts
.agent/
.vercel

# aw-tester captured auth sessions (secrets — never commit)
.auth/

# Deno lockfile — the edge functions are checked with `deno info`/`deno check`
# ad hoc; we do not pin a lockfile for them.
deno.lock
